import 'package:flutter/material.dart';

class SlotsViewBody extends StatelessWidget {
  const SlotsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}